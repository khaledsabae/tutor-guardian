# ⚠️  THIS WORKFLOW IS INTENTIONALLY DISABLED — use backend.yml instead.
#
# backend.yml covers the same KB integrity + pytest tasks with:
#   - Path filtering (doesn't run on mobile-only changes)
#   - cancel-in-progress concurrency control
#   - A separate fast KB integrity job
#
# To re-enable, restore the `on:` block below. We keep this file only to
# avoid breaking any existing branch protection rule that references "CI".

name: CI (disabled — see backend.yml)

on:
  workflow_dispatch:  # Manual trigger only — not automatic

jobs:
  redirect:
    name: Use backend.yml
    runs-on: [self-hosted, production]
    steps:
      - name: Notice
        run: echo "This workflow is disabled. Use backend.yml for CI."
