# ── Tutor Guardian fine-tuned model for Ollama ────────────────────────
# Instructions:
#   1. Place tg-tutor.gguf in the same directory as this Modelfile
#   2. Run: ollama create tg-tutor -f Modelfile.tg-tutor
#   3. Test: ollama run tg-tutor
#
# The GGUF is generated by the Colab notebook at ops/colab/finetune_tg.ipynb
# from a Qwen2.5-1.5B-Instruct or Qwen2.5-3B-Instruct base.

FROM ./tg-tutor.gguf

# ── Template (Qwen chat format) ───────────────────────────────────────
TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}<|im_start|>user
{{ .Prompt }}<|im_end|>
<|im_start|>assistant
{{ .Response }}<|im_end|>"""

# ── System prompt ─────────────────────────────────────────────────────
SYSTEM """أنت مساعد تربوي ذكي للأهل العرب المسلمين.
أجب على آخر سؤال في المحادثة فقط.
استخدم المعلومات المقدمة لك ولا تختلق معلومات.
لا تكرر الإجابة على أسئلة سابقة."""

# ── Generation parameters ─────────────────────────────────────────────
PARAMETER temperature 0.3
PARAMETER top_p 0.9
PARAMETER top_k 40
PARAMETER repeat_penalty 1.1
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|im_start|>"
