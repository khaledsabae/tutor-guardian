# برومبت الجلسة الجاية — استكمال خطة نمو «المربي الذكي»

انسخ من هنا لتحت في جلسة جديدة:

---

كمّل تنفيذ خطة النمو في `tutor-guardian/plans/growth-master-plan-2026-07.md`. جلسة 2026-07-17 أغلقت: §4.3 onboarding أقل من 90 ثانية (e4c406f)، §6.4 لحظة الإحالة الصحيحة (883df57)، §6.5 تقييم الـLoRA (تفوقت 5-2-3 — `plans/lora-eval-2026-07-17.md`)، وتحليل الهند §6.2 + اقتراح وكيل ردود المراجعات كمستندي قرار. الحالة: analyze صفر أخطاء/تحذيرات، تستات 151/151، ARB 909/909.

**قرارات معلقة لخالد (اعرضها عليه أول الجلسة ولا تنفذ قبل رده):**
1. ⚪ تبديل نموذج الإنتاج: `OLLAMA_PRIMARY_MODEL=tg-tutor:v4` → `tg-tutor:v6` في env باكند الـVPS + ريستارت (v6 جاهز ومختبر على tg-home). التوصية: نعم + مراقبة أسبوع عبر ops-llm/OMAR.
2. ⚪ الهند: تفعيل نسخة المتجر الإنجليزية الجاهزة في Play Console (يدوي) — لا ترجمة محتوى (راجع `plans/india-decision-analysis-2026-07.md`).
3. ⚪ وكيل ردود المراجعات: إنشاء service account الآن وبناء الوكيل عند أول مراجعة (راجع `plans/review-reply-agent-proposal.md`).

**شغل تقني متاح بعد القرارات:**
- إصدار v1.0.28: يشحن onboarding الجديد + بطاقة الإحالة + الواجهة الإنجليزية (بناء AAB والرفع يدوي).
- تضييق قواعد R8 في `mobile/android/app/proguard-rules.pro` (معدلات التحسين 38-39% بسبب keep rules شاملة لـio.flutter.** وFirebase) — يحتاج اختبار جهاز حقيقي قبل الإصدار، لا يُدمج بلا تحقق.
- مشاكل Play Console الأخرى المُبلغة على 70: edge-to-edge مُصلح منذ +69؛ تحذيرا setStatusBarColor وBitmapFactory من محرك Flutter/plugins — استشاريان، غير قابلين للحل من طرفنا.

قواعد ثابتة: التطبيق مجاني للأبد — ممنوع monetization · auto-deploy على أي push (~6 دق + 502 warm-up طبيعي) · `ops/scripts` على الـVPS بـ`git pull` بلا rebuild · healthy ≠ exit 0 · أي تغيير تشغيلي يتسجل في OPERATIONS_LOG · chromadb ممنوع ترقيته (`<1`) · بنود ⚪ قرارات لخالد وحده.
