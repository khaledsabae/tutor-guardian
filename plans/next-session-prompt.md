# برومبت الجلسة الجاية — استكمال خطة نمو «المربي الذكي»

انسخ من هنا لتحت في جلسة جديدة:

---

كمّل تنفيذ خطة النمو في `tutor-guardian/plans/growth-master-plan-2026-07.md`. كل مربعات المراحل 1 و2 وسبرينت المحتوى/i18n مقفولة ومتحققة بالأثر (راجع الخطة نفسها + `publishing-center/OPERATIONS_LOG.md` قيود 2026-07-16). النسخة v1.0.27+71 اترفعت على Play Console. المتبقي من الخطة بالترتيب اللي عايزك تنفذه:

1. **§4.3 — Onboarding يوصل لأول قيمة خلال أقل من 90 ثانية** (أهم بند متبقي):
   سؤال واحد فقط («عمر طفلك؟») → عرض فوري لنصيحة مخصصة + درس مقترح، وتأجيل كل ما عدا ذلك (الاسم، الجنس، التفاصيل) لما بعد أول قيمة. قِس بأحداث القمع الموجودة (`install → onboarding_done → child_added → first_chat OR first_lesson`). اشتغل في `mobile/lib/features/onboarding/` والتزم بنمط i18n القائم (كل نص جديد = مفتاح في app_ar.arb + app_en.arb، التوازن 887/887 لازم يفضل متوازن). بوابات الجودة: flutter analyze صفر أخطاء + flutter test 145/145.

2. **§6.4 — لحظة طلب الإحالة الصحيحة:**
   اعرض بطاقة «ادعُ صديقًا» فقط في لحظات الفخر: بعد إتمام سلسلة عادات 7 أيام، أو بعد قصة أعجبت الطفل. الباكند جاهز (`/api/referral/me` بيرجع share_url وreward_coins=100 — الحملة المضاعفة مفعّلة). الشغل موبايل فقط.

3. **§6.2 — قرار الهند (سوق #2 بلا خدمة):**
   الآن بعد اكتمال i18n (887 مفتاح إنجليزي جاهز) صار ممكنًا عمليًا. حضّر تحليل قصير: تكلفة تشغيل نسخة إنجليزية (المحتوى الشرعي/القصص/الدروس ما زالوا عربي!) مقابل أثرها، واعرض التوصية على خالد قبل أي تنفيذ — القرار له.

4. **§6.5 — تقييم LoRA المدربة:**
   قيّم `tg-tutor-commandr-lora` مقابل tg-tutor:v4 على مجموعة أسئلة والدين حقيقية (خد عينة من chat_sessions أو من أسئلة الألم في `_TOPIC_SEEDS`). انتبه: v5 مسجّل على tg-home لكن بيعمل timeout على CPU — القياس لازم يكون بظروف الإنتاج الفعلية. لو ماكسبتش بفارق واضح، سيبها واقفل البند بالسبب.

5. **§4.3/§6.1 — الرد على مراجعات المتجر خلال 48 ساعة:**
   بند تشغيلي مستمر — اقترح أتمتة خفيفة: وكيل PCC يسحب المراجعات الجديدة يوميًا ويبعت مسودات ردود على تلجرام لخالد للموافقة (مفيش نشر تلقائي).

**قواعد ثابتة (من CLAUDE.md والذاكرة):**
- التطبيق مجاني للأبد بلا إعلانات — ممنوع أي monetization.
- tutor-guardian بيعمل **auto-deploy على أي push للـmain** (~6 دقايق + 30-90 ثانية warm-up بيرجع فيها 502 — طبيعي). ماتعملش push إلا لما تكون جاهز للنشر.
- `ops/scripts` على الـVPS bind-mount read-only من `/root/tutor-guardian` — إصلاحاتها بـ`git pull` بدون rebuild.
- healthy ≠ exit 0: أي تحقق بالأثر الفعلي.
- أي تغيير تشغيلي → قيد في `publishing-center/OPERATIONS_LOG.md` + commit.
- chromadb مثبت `<1` — ممنوع ترقيته (volume الإنتاج 0.6).
- بنود ⚪ (شراكات مجتمعية، تمويل وقفي، ترقية عتاد) قرارات لخالد وحده — ماتنفذهاش.
