# المحتوى الإنجليزي — أين وصلنا وما الذي يلزمه بشر

> آخر تحديث: ٢٠٢٦-٠٨-١٣ · الفرع `feat/en-media` (+ `feat/en-games`، `feat/en-stories`)
> **لم يُدفَع شيء إلى `main`.** دفع `main` = نشر تلقائي على prod.

## لماذا هذا الشغل أصلًا

٢٧٪ من المستخدمين على أجهزة إنجليزية و١١٪ فرنسية — ٤٠٪ من مستخدمي المساعد على
أجهزة غير عربية. وبلّغ مستخدمان من داخل التطبيق (٢٠٢٦-٠٨-٠٢ و٠٨-٠٣): «أغير
الإعدادات إلى اللغة الإنجليزية لكن الدروس باللغة العربية».

---

## ١. ما كان مكسورًا فعلًا وقت البدء (مُتحقَّق لا مُفترض)

| # | العطل | الأثر |
|---|---|---|
| ١ | `get_daily_tips()` بلا `lang`، و`daily_tips` غائبة من حلقة التراكب | **٢١٠ نصيحة مترجمة ومدفوعة** على القرص لا يصل منها شيء |
| ٢ | استنتاج لغة الميديا: `if "_ar" in fname … else "en"` | بودكاست عربي (٣٧٫٨ ميجا) **يُقدَّم لمستخدمي الإنجليزية على أنه إنجليزي**، والرجوع للعربي لا يعمل |
| ٣ | `contentLanguageProvider` افتراضه `'ar'` ثابتًا، منفصل عن لغة الواجهة | مستخدم على جهاز إنجليزي يقرأ إنجليزي **ويسمع عربي** — وكان سيستمر كذلك بعد إنتاج الصوت الإنجليزي |
| ٤ | `search()` يطابق الذاكرة العربية فقط | بحث «sleep» = **صفر نتيجة**؛ المنهج الإنجليزي غير قابل للوصول إلا بالتصفّح |
| ٥ | `cron_gen_podcasts.sh` ينادي `scripts/gen_podcasts_cron.py` المحذوف منذ `b704d67` | كرون **يبدو سليمًا في اللوج ولا يولّد شيئًا** (`set -u` لا `set -e`) |
| ٦ | بوابة المدة في `validate_podcasts.py` | **لم تُطلق ولا مرة**: الملفات حاويات MP4 فـ`mutagen.mp3.MP3` يرمي، و`dur=None` تمرّ على الحجم وحده |
| ٧ | المترجم اخترع حقل `reflection_prompts` في ١٣ نصيحة و٣ مسارات | نصائح يومية تُعرض ومعها أسئلة تأمّل لا أصل لها |
| ٨ | حارس المسرد يطابق بالاحتواء | «haya» داخل «al-Hayah» → رفض ترجمة **سليمة** مرتين |
| ٩ | مخطّطات JSON بـ`additionalProperties:false` بلا `translation` | **كل ملف إنجليزي يفشل مخطّطه** |

كلها مُصلَحة. رقم ٥ و٦ لم يكونا في الخطة — ظهرا أثناء التنفيذ.

---

## ٢. حالة الترجمة النصية

«٣٦٢ ملاحظة» كانت **سجلات بائتة**: مراجعة ٢٠٢٦-٠٨-١١ نزلت إصلاحاتها ولم يُعَد
تشغيل المراجع، فـ١٩٪ من السجلات تقتبس إنجليزيةً لم تعد موجودة. أُعيد الحكم على
النص الحالي أولًا (٤٢٤ ملفًا)، ثم ثلاث تمريرات إصلاح:

| | البداية | النهاية |
|---|---|---|
| high | ٢٠ | **٣** |
| medium | ٨٥ | **١٣** |
| low | ٢٣٢ | ١١٠ |
| **المجموع** | **٣٣٨** | **١٢٩** |

**٢١١ إصلاحًا رُفض عند البوابات ولم يُكتب.** البوابات الثلاث: تطابق بنيوي ·
قيد المسرد البرمجي · إعادة مراجعة بنموذج من عائلة أخرى يجب أن يجد عيوبًا أقل.

---

## ٣. 🕌 ما يلزمه بشر — لا تُنشر الإنجليزية قبله

1. **`approved_by: null` على ٤٢٤ ملفًا.** والمخطّط نفسه يقول:
   «null = غير مراجَع، **لا يُنشر للإنجليزية**». المستودع يناقض نفسه في الإنتاج
   منذ `69e77b7`. قرارك كان: يبقى شغّالًا وتُصلَّح الملاحظات — وقد صُلِّحت.
   **لكن التوقيع نفسه ما زال ناقصًا، ولا أداة هنا تكتبه.**
2. **٣ ملاحظات high باقية**، إحداها ليست عيب ترجمة أصلًا:
   `tip_16-18_016` العربية فيها «سفينة» والسياق يقتضي «سكينة» — **الإنجليزية
   ترجمت الخطأ بأمانة. التصحيح على الجانب العربي.**
3. **البودكاست العربي القائم (٢١٤ حلقة) لم يخضع لأي تحقّق شرعي.** NotebookLM
   يعيد الصياغة بحرية. الإنجليزي لن يكون أسوأ لكنه يوسّع تعرّضًا قائمًا.

---

## ٤. الصوت والفيديو الإنجليزي — جاهز للتشغيل، لم يُشغَّل

المسار كله صار يقبل `--lang en`، ولم أشغّله لأنه **يحتاج جلستك أنت**:
NotebookLM يستوثق بكوكيز Chrome على اللابتوب، والحصّة تنقّط يوميًا.

```bash
scripts/cron_gen_podcasts.sh en          # ١٧٠ حلقة
python3 scripts/gen_path_videos_cron.py --lang en --dry-run
```

**زمن واقعي مقيس من التاريخ لا مقدَّر:** ٢١٤ حلقة عربية أُنتجت على **١٣ يوم
توليد فعلي** (~١٦/يوم، ذروة ٣٦) ممتدة على ~٧ أسابيع بسبب الحصّة.
فالإنجليزي ≈ **١١–١٤ يوم توليد، ٣–٦ أسابيع زمن جدار**.

🎉 المصادر **مرفوعة سلفًا** (١٦٩ معرّف مصدر في نفس الدفتر)، فالتوليد إعادة نداء
لا مشروع رفع. السعة: ~٧ جيجا إضافية، والـVPS فيه ٧٤ جيجا حرة.

**التسمية:** العربي يبقى `<lesson_id>_podcast.mp3` بلا وسم (إعادة تسميته =
٧٫٣٥ جيجا rsync بلا مقابل)، والإنجليزي `_podcast_en.mp3`. غياب الوسم = عربي.

---

## ٥. القرآن لا يُترجَم

قاعدتك صارت **حارسًا على pre-commit** لا تعليمةً في وصف:
`ops/tools/check_quran_rendering.py`.

يمسك الشكل الذي يقدّم إنجليزيةً على أنها قرآن: حروف لاتينية داخل ﴿ ﴾ · اقتباس
إنجليزي بعد «Allah says:» بلا إعلان أنه تفسير · عنصر `kind: verse` بإنجليزية
بلا `provenance` من ترجمة منشورة. ومعه self-tests تُبطله بـ`exit 2` (٤ حالات
لازم يمسكها، ٥ لازم يمرّرها).

**المنهج الإنجليزي نضيف اليوم — لا ملف واحد من ٤٢٤ يعرض آية بالإنجليزية.**
الحارس موضوع الآن عشان يفضل كده لما تُترجَم الأذكار (**١٤٢ آية**) — لا بعدها.

⚠️ **حدّه:** يمسك الشكل لا الأمانة. تفسير مُعلَن لكنه محرَّف يمرّ — تلك مراجعة
أهل العلم، والحارس لا يدّعي أنه يغني عنها.

---

## ٦. القصص والألعاب والأذكار

**القصص — ١٤/١٤ مترجمة، ٨٠/٨٠ صفحة محفوظة.** الرسوم (٩٤ صورة) وفيديوهات الغلاف
صامتة وخالية من النص، فأُعيد استعمالها كما هي — لا توليد صور جديد.

🚨 **وهنا ظهر عيب لا يستطيع الفحص الشكلي رؤيته.** خمس قصص فيها آيات. النسخة
الأولى من `aisha_permission` كتبت الآية **صحيحةً بالعربية داخل ﴿﴾** — يمرّها
`check_quran_rendering` لأنها سليمة شكلًا — لكن الجملة قبلها تجعلها تُقرأ **كأنها
كلام الأب** لا قوله تعالى، إذ سقط «قوله تعالى». والمراجع العدائي لمّا أُعيد
سؤاله أعاد نفس الملاحظة بدرجة **أقل** (high → medium)، أي أن بوابة تثق بتصنيفه
يستطيع أن يتجاوزها. فأُضيفت بوابة **حتمية**: لو العربية تنسب الآية، فالإنجليزية
لازم تسمّي الله أو القرآن في نفس الحقل. والآن: «…the words of Allah: ﴿…﴾.
Interpretation of the meaning: …».

**الألعاب والأذكار — استُخرج ٤٬٤٦٦ سطر نص من Dart إلى JSON، بالعربية فقط وبلا
أي تغيير مرئي.** ٤ ألعاب (٢٠٠ سؤال، ٨٠٠ خيار) + ٢٨١ عنصر أذكار + ٣٧ محطة رحلة.
البرهان: أداة تُعيد توليد كود Dart من الـJSON وتقارنه **بايت ببايت** بالأصل عبر
`git show` — **٧/٧ مناطق متطابقة، ٢٠٠٬٩٦٢ بايت**.

والحرّاس نُقلوا للـJSON وقورنوا **جنبًا إلى جنب** بالنسخة القديمة (نفس ١٤٢ آية،
نفس ١٥ حديثًا، نفس ٢٨١ عنصرًا بنفس الترتيب)، ثم أُعيد تمثيل ثماني حالات فشل
للتأكد أنهم ما زالوا يعضّون. وصاروا **أشدّ** في موضعين: حزمة فارغة كانت تُقرأ
«✅ 0/0»، والإسناد صار يُشتقّ من العربية ويُرفض إذا خالفها.

🚨 **إشعارات الأذكار كانت تحمل رقم موضع في القائمة** (`adhkar_$idx`) وتُجدوَل
أيامًا مقدَّمًا — فأي إعادة ترتيب تُحوِّل إشعارًا إلى **آية غير التي قُصدت**. صارت
تحمل معرّفًا ثابتًا، مع فرع يقرأ الأرقام القديمة لإصدار واحد لأن أجهزة المستخدمين
فيها طابور مجدوَل ١٤ يومًا.

**وحدات المعرفة:** حقل `language` أُضيف (٤١ en · ٧٥٤ ar · ٦٩ mixed) بعد أن كان
يُسقَط صامتًا. **والترشيح لم يُضَف عمدًا**: ٤٣ وحدة إنجليزية فقط، فترشيح صارم
يُفرغ النتيجة ويرجع «لا توجد معلومات كافية» بدل استشهاد عربي مفيد. تفضيلٌ حقيقي
يحتاج تمرير لغة السؤال عبر مسار الإجابة الحيّ — تغيير ترتيب لا إضافة حقل.

---

## ٧. ما لم يُنفَّذ ولماذا

- **لم يُدفَع إلى `main`** — نشر تلقائي على prod، وأنت نائم. الفرع جاهز.
- **لم تُولَّد ميديا إنجليزية** — يحتاج جلسة Chrome حيّة وأسابيع حصّة.
- **١٦ ملخّص درس يتجاوز حدّ ٨٠٠ حرف** — والعربية تتجاوزه أيضًا (حتى ١٬٣٢٢)،
  فالحدّ بائت في اللغتين لا عيب ترجمة. تُركت.
- **٣ سجلات مراجعة بـ`severity: null` وبلا تعليل** — بلا معلومة يُتصرّف بها،
  ولم أحذف ناتج مراجع بصمت.
- **`age_group` enum في المخطّط ما زال `حتى عام`** بينما البيانات `0-3`/`2-3`،
  و`domain` ينقصه `aqeedah` — ١٦٠ ملفًا عربيًّا يفشل مخطّطه. **سابق للترجمة
  ومستقل عنها**، فلم ألمسه.
- **~٢٠ نصًّا في الألعاب باقية في Dart** (`EduGameTheme.name/description`
  و`GameTier`) — وسائط `const` لا تصل إليها حزمة غير متزامنة، ومكانها الصحيح
  ARB لا حزمة المحتوى.
- **`isRightSwipe: true` في قارئ القصص** — اتجاه قلب الصفحة اتجاهيٌّ مثل النص،
  والكتاب الإنجليزي يُقلَب عكسيًّا. تُرك لأن دلالة الحزمة لم تُتحقَّق بلا جهاز.

---

## ٨. الحالة النهائية — مُتحقَّقة بالتشغيل

| الفحص | النتيجة |
|---|---|
| تستات الباكند | **٧١٩ ناجحة** · exit 0 |
| `flutter analyze` | **No issues found** · exit 0 |
| `flutter test` | **٢٩٩ ناجحة** · exit 0 |
| حرّاس المحتوى الخمسة | كلها exit 0 (١٤٢/١٤٢ آية · ١٥/١٥ حديثًا · ٢٨١ عنصرًا · ٤٢٦ ملفًا إنجليزيًّا) |
| برهان الاستخراج | ٧/٧ مناطق متطابقة بايت ببايت |
| ruff | نظيف |

**الفرع: `feat/en-media`** (مدموج فيه `feat/en-games` و`feat/en-stories`).
الدفع لك — `git push origin feat/en-media` أو الدمج في main حين تقرّر.
